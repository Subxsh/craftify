{
  "name": "craftify",
  "version": "1.0.0",
  "description": "Craftify - E-commerce platform for handmade products",
  "scripts": {
    "build": "cd E-commmerce/frontend && npm install && npm run build",
    "vercel-build": "cd E-commmerce/frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "form-data": "^4.0.5"
  }
}
